{
  "author": "Andreas Lind Petersen <andreas@one.com>",
  "name": "httperrors",
  "description": "Handy JavaScript Error classes representing the standard HTTP errors. Easily extensible and subclassable.",
  "keywords": [
    "http",
    "errors",
    "error",
    "request",
    "proxy",
    "client",
    "class",
    "errorclass"
  ],
  "version": "0.4.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/One-com/node-httperrors.git"
  },
  "main": "lib/httpErrors.js",
  "engines": {
    "node": ">= 0.2.0"
  },
  "dependencies": {
    "createerror": "0.4.1"
  },
  "scripts": {
    "dist": "browserify ./lib/httpErrors.js -s httpErrors | uglifyjs -mc > ./dist/httpErrors.js",
    "test": "mocha --recursive"
  },
  "devDependencies": {
    "browserify": "^5.3.0",
    "mocha": "1.12.1",
    "uglifyjs": "^2.3.6",
    "unexpected": "=0.0.6"
  },
  "publishConfig": {
    "repository": "http://registry.npmjs.org/"
  }
}
